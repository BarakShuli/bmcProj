import { SortArrayByPropertyPipe } from './sort-array-by-property.pipe';

describe('SortArrayByPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArrayByPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
